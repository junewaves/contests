#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
void solve() {
    int n, m;
    cin >> n >> m;
    vector<int> str(n);
    string med = "0";
    for (int i = 1; i < m; i++)
        med += '1';
    for (int i = 0; i < n; i++)
        cin >> str[i];
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int _n;
    cin >> _n;
    while (_n--) {
        solve();
    }
    return 0;
}